# AGENTS.md - 像素工厂 Logic 编程学习项目规范

> 本文件是持久化记忆文件，记录项目的所有规范与约定，后续所有工作都应遵循此文件。

---

## 1. 项目概述

当前正在学习像素工厂（Mindustry）的 Logic 汇编编程能力。项目目标是系统性地整理所有 Logic 积木的用法、源码实现要点、代码实战示例，以及高级主题（网络同步、帧差等）。

---

## 2. 文件与文件夹命名规范

- 所有文件和文件夹的命名格式为：**数字 + 短滑杠 + 名字**（如 `01_ubind.md`、`02_Control Flow`）
- 数字代表该文件/文件夹在整合时的扫描顺序
- 文件夹内的代码实战文件同样遵循此规范

### 整合扫描顺序

后续将使用 Python 脚本对整个文件夹内容进行整合，合并为一个 MD 文件。扫描规则：
1. 先扫描当前层级的单个文件（按文件编号排序）
2. 再扫描文件夹（按文件夹编号排序），递归进入文件夹内部按同样规则扫描

---

## 3. 积木教程编写规范

### 3.1 内容要求
- 表述清晰，保留重要内容，隐藏没有必要的代码实现细节
- 介绍游戏源码内的方法时，尽量先用中文表示其意图，然后在括号内标出英语（如"设置瞄准控制模式（`ai.aimControl`）"）。简单内容可只保留中文
- 积木介绍中不要出现对编写 Logic 没有营养的内容
- 每个积木都应有用途标注；如果用途单一且显而易见，可以不加标注

### 3.2 代码实战规范
- 所有"代码实战内容"单独放入一个文件夹存放，并对文件夹进行编号处理
- 代码实战文件使用 `.txt` 格式，确保可以直接导入游戏

### 3.3 代码编写规范
- 所有代码示例使用**小驼峰命名法**（camelCase）
- 临时变量用 `_` 开头（如 `_tempResult`）
- 秉持高可读性原则

### 3.4 注释规范
- Logic 内没有注释指令，但可以使用 `print` 进行标注
- `print` 注释会污染打印信息缓存，还可能增添逻辑运行负担
- 如果必须使用 `print` 注释，应放在永远无法执行到的位置或执行次数极少的位置，且该逻辑不打印信息
- 在游戏外编写 `##` 注释打入游戏后会自动清除（LParser 中 `#` 字符触发注释读取，注释不创建语句），适合在游戏外浏览
- **源码验证结论**：`##` 注释不会影响行号跳转的 jump。LParser 中 `#` 触发 `comment()` 方法读取至行尾或 EOF，注释行不生成语句、不占用指令索引，因此不影响任何形式的 jump（包括行号跳转和标签跳转）

---

## 4. 跳转标签规范

### 4.1 使用标签跳转，不使用行号跳转
- 在编写 Logic 时，**统一使用跳转标签（label）进行跳转**，不使用行号跳转
- 行号跳转维护困难，插入或删除指令后行号会偏移

### 4.2 标签踩空问题（重要）
- 如果跳转标签在一行积木的最尾部（即标签下方没有任何积木），该标签在导入游戏后将无法正常跳转到目标位置
- 在游戏内编辑器中，跳转目标是选择积木来定位的，标签下方没有积木就无法选中
- **源码层面**：LParser 中标签存储当前 `line` 值（语句索引），若标签在文件末尾无后续语句，`line` 等于 `statements.size()`，跳转到此位置时 counter 超出指令数组范围，`runOnce()` 会将其重置为 0（即跳回开头）。虽然功能上等效于 `end`，但实际游戏中可能存在不可靠的行为

### 4.3 正确做法
- 标签下方必须紧跟至少一条积木指令
- 如果需要跳转到程序末尾（即重新开始），直接使用 `end` 指令，`end` 会将 counter 设为 `instructions.length`，下一帧 `runOnce()` 会重置为 0
- 不要在文件末尾放置裸标签

---

## 5. 代码导入验证规范

- 所有编写出来的代码都应该先进行模拟导入验证，确保能被 LParser 正确解析
- 验证要点：
  - 指令名称拼写正确
  - token 数量符合要求（每条指令的参数数量固定）
  - 枚举类型参数必须使用有效的枚举值（如 BlockFlag 必须是 `core`/`storage`/`generator` 等，不能用 `0`）
  - 跳转标签有对应的定义且不踩空
  - 变量名符合规范

### ulocate 格式特别说明
- ulocate 始终为 9 个 token：`ulocate <locate> <flag> <enemy> <ore> <outX> <outY> <outFound> <outBuild>`
- **`flag` 参数（第2个参数）必须是有效的 BlockFlag 枚举值**（如 `core`、`battery` 等），即使 locate 模式为 `ore`/`spawn`/`damaged` 时也必须填写有效值
- 常见错误：在 ore 模式下将 flag 填为 `0`，导致 `BlockFlag.valueOf("0")` 抛出异常，语句变成 InvalidStatement（灰色积木）

---

## 6. 帧差检查规范

### 6.1 什么是帧差
每个逻辑处理器在一帧（tick）内执行相应数量的逻辑指令（由 `@ipt` 决定）。不同处理器在同一帧内按顺序执行，先执行的处理器完成所有指令后，后执行的处理器才开始。

### 6.2 帧差的实际表现
以 `ucontrol flag` 为例：
- **写入**：`unit.flag = p1.num()` — 直接字段赋值，无缓存，立即生效
- **读取**：`sensor @flag` — 直接读取 `unit.flag` 字段，无缓存

当两个处理器 A 和 B 在同一帧内对同一单位执行：
1. 处理器 A 执行：`ucontrol flag 1`，然后 `sensor flagVal @unit @flag` → 读到 1
2. 处理器 B 执行：`ucontrol flag 2`，然后 `sensor flagVal @unit @flag` → 读到 2

两个处理器都读到了自己设置的值，原因是**执行顺序**：A 在 B 之前执行，A 写入并读取完成后 B 才开始执行。这不是因为缓存未同步，而是因为处理器在同一 tick 内顺序执行。

### 6.3 帧差检查清单
编写每个积木时，应检查以下问题：
- [ ] 该指令的写入是否立即生效？（直接字段赋值 vs 缓存/延迟生效）
- [ ] 该指令的读取是否读取实时值？
- [ ] 两个处理器在同一帧内操作同一目标时，是否会产生不可预期的结果？
- [ ] 该指令是否有 40 tick 缓存机制？（如 uradar、ulocate）
- [ ] 该指令是否涉及网络同步？（如 control、write 对建筑的操作）

### 6.4 已确认无帧差问题的指令
- `ucontrol flag`：直接字段赋值，无缓存。帧差表现为执行顺序差异，非缓存问题
- `ucontrol move`/`stop`/`approach` 等：设置 LogicAI 的控制参数，单位在后续物理更新中响应
- `ubind`：直接设置 `@unit`，无缓存

### 6.5 已确认有缓存机制的指令
- `uradar`：40 tick 缓存，重复执行返回旧结果
- `ulocate`：40 tick 缓存，重复执行返回旧结果
- `radar`（建筑版）：同样有缓存机制

### 6.6 控制状态（Control State）机制

**核心规则**：所有 ucontrol 子指令、ulocate 在执行前都会调用 `checkLogicAI()`，将单位的控制器替换为 LogicAI（如果尚未替换），并刷新控制计时器（`controlTimer = logicControlTimeout`，即 10 秒）。10 秒内若无新的 ucontrol/ulocate 指令执行，单位将恢复原 AI。

**指令分类**：
- **ucontrol 所有子指令**（含 flag、getBlock、within、unbind 等）：调用 `checkLogicAI()` **且**刷新 `controlTimer`
- **ulocate**：调用 `checkLogicAI()` **且**刷新 `controlTimer`
- **uradar**：调用 `checkLogicAI()` 但**不刷新** `controlTimer`。若仅使用 uradar 而不执行其他 ucontrol/ulocate 指令，单位将在 10 秒后恢复原 AI
- **ubind**：不调用 `checkLogicAI()`，不触发控制状态

**玩家单位**：`Player.isLogicControllable()` 返回 `false`，因此 ucontrol/uradar/ulocate 对玩家控制的单位无效（`checkLogicAI` 返回 null）。

**文档编写要求**：每个 ucontrol 子指令的文档中应标注"会触发控制状态"。

### 6.7 wait 指令同步机制
- `wait` 指令的 `curTime`（已等待时间）和 `accumulator`（执行点数累积器）均被序列化同步
- `write()` 方法中显式保存所有 WaitI 实例的索引和 curTime 值，以及 accumulator 值
- `read()` 方法中反序列化这些值并恢复
- 多人模式下可能因 `Time.delta` 在不同客户端间的微小差异导致 wait 计时存在轻微偏差，但状态本身是同步的

---

## 7. 网络同步规范

### 7.1 同步问题概述
像素工厂是联机游戏，Logic 在多人模式下会出现同步问题。同步问题主要出现在**输入输出（IO）分类**的积木上。

### 7.2 不需要担心同步的指令
以下指令由服务器逻辑直接操作，不涉及客户端下达，即使有不同步也没有关系：
- `ubind`（单位绑定）
- `ucontrol`（单位控制）所有子指令
- `uradar`（单位雷达）
- `ulocate`（单位定位）

### 7.3 需要注意同步的指令
以下指令涉及建筑状态读写，在多人模式下可能出现同步问题：
- `read`/`write`（读写内存建筑）
- `control`（控制建筑）
- `sensor`（传感器，读取建筑状态时）
- `draw` 系列指令（绘制到显示屏）

### 7.4 文档编写要求
- 在涉及 IO 和建筑控制的积木文档中，应标注网络同步注意事项
- 在单位控制类积木文档中，可简要说明"由服务器逻辑直接操作，无同步问题"

---

## 8. 处理器类型与执行速度

| 处理器 | @ipt（每 tick 指令数） | 链接范围 | 尺寸 | 备注 |
|--------|----------------------|----------|------|------|
| 微型处理器（micro-processor） | 2 | 10 格 | 1x1 | 最基础 |
| 逻辑处理器（logic-processor） | 8 | 22 格 | 2x2 | — |
| 超级处理器（hyper-processor） | 25 | 42 格 | 3x3 | 需要冷却液 |
| 世界处理器（world-processor） | 8（默认，可通过 setrate 修改，最大 1000） | 全图 | 1x1 | privileged，全图范围 |

### 执行模型
- 每帧累积执行点数：`accumulator += edelta() * ipt`
- 当 `accumulator >= 1` 时执行一条指令，`accumulator--`
- 累积上限为 `maxInstructionScale * ipt`（默认 maxInstructionScale=5），防止累积过多
- `wait` 指令会设置 `yield = true`，中断当前 tick 的执行
- `@ipt` 环境变量可在逻辑中读取当前处理器的每 tick 指令数

---

## 9. Logic 积木分类

游戏内积木按 LCategory 分为以下类别：

| 分类 | 英文名 | 说明 |
|------|--------|------|
| 控制程序 | control | 控制流指令（set, op, jump, end, wait 等） |
| 操作 | operation | 操作指令（sensor, control, radar, getlink 等） |
| 输入输出 | io | 输入输出指令（print, draw, read, write 等） |
| 控制方块 | block | 方块控制指令（setblock, getblock 等） |
| 控制单位 | unit | 单位控制指令（ubind, ucontrol, uradar, ulocate） |
| 世界 | world | 世界处理器专属指令（setrule, setprop, spawn 等） |

---

## 10. 学习大纲

1. **先搞懂每个积木是干什么用的**：逐个学习每个分类下的积木，理解其功能、参数、源码实现
2. **理解网络同步问题**：由于像素工厂是联机游戏，Logic 会出现同步问题，主要出现在输入输出分类。学习如何利用和解决同步问题
3. **学习世界处理器内容**：世界处理器（privileged processor）拥有特殊权限，可以执行普通处理器无法执行的指令

---

## 11. 项目目录结构

```
mindustry logic skill/
├── AGENTS.md                          # 本文件（项目规范）
├── logic skill/
│   ├── 00_Logic入门与学习大纲.md       # Logic 基础介绍与学习大纲
│   ├── 01_Control Unit/               # 控制单位分类（已完成）
│   │   ├── 01_ubind.md
│   │   ├── ...
│   │   └── 代码实战/
│   ├── 02_Control Flow/               # 控制程序分类
│   ├── 03_Operations/                 # 操作分类
│   ├── 04_IO/                         # 输入输出分类
│   ├── 05_Block Control/              # 控制方块分类
│   ├── 06_World/                      # 世界分类（世界处理器专属）
│   └── 07_实战案例/                    # 实战案例（兵控逻辑、挖矿逻辑、多核优化等）
```

---

## 12. 大型表格生成规范

### 使用 Python + 游戏源码打表

在编写 Logic 教学文档时，如需引用游戏内的大型表格数据（如建筑物品需求列表、单位属性数据、处理器参数对比表、BlockFlag 枚举值等），**必须使用 Python 脚本配合游戏源码自动生成**，而非手动编写。

原因：
1. **准确性**：游戏版本更新时数据会变化，手动编写极易出错或过时
2. **完整性**：游戏源码中有数百个建筑和单位，手动列举容易遗漏
3. **可维护性**：游戏更新后只需重新运行脚本即可刷新表格

### 打表流程
1. 编写 Python 脚本，解析游戏源码（Java 文件或编译后的 class/jar）
2. 提取所需数据（如建筑的 `itemCapacity`、`health`、`requirements` 等字段）
3. 生成 Markdown 表格格式
4. 将生成的表格粘贴到教学文档中
5. 在表格旁标注数据来源的游戏版本号
